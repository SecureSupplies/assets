services:
  phase8-government-biz:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - .:/app
    env_file:
      - .env
    command: bash -lc "pip install -r requirements.txt && python run_pipeline.py --mode test --include-samples"
